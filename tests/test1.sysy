int a = 10, b = 5, c;
int d = 10;

int f(int a, int b) {
    int c = d;
    return a + b + c;
}

int main() {
    int d = 10;
    {
        int d = 100;
        c = b + d;
    }

    {
        int d = 1000;
        return a + c + d;
    }
}
